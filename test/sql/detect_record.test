# name: test/sql/detect_record.test
# description: test detect_record extension
# group: [detect_record]

# Before we load the extension, this will fail
statement error
SELECT detect_record('Sam');
----
Catalog Error: Scalar Function with name detect_record does not exist!

# Require statement will ensure this test is run with this extension loaded
require record

# Confirm the extension works
query I
SELECT detect_record('Sam');
----
TODO: detect_record Sam 🐥
