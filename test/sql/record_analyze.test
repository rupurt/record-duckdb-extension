# name: test/sql/record_analyze.test
# description: test record_analyze extension
# group: [record_analyze]

# Before we load the extension, this will fail
statement error
SELECT * FROM record_analyze('Sam', '2', '3');
----
Catalog Error: Table Function with name record_analyze does not exist!

# Require statement will ensure this test is run with this extension loaded
require record

# Confirm the extension works
query I
SELECT * FROM record_analyze('Sam', '2', '3');
----
TODO: record_analyze Sam 🐥
